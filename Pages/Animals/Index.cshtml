@page
@model PetAdopt.Pages.Animals.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Animal[0].animal_speacies)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animal[0].animal_breed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animal[0].animal_age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Animal[0].animal_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Animal) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.animal_speacies)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.animal_breed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.animal_age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.animal_name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
